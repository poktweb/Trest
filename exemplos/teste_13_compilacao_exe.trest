# Teste 13: Compilação para Executável
# Este arquivo será usado para testar compilação para .exe

печать("=== Teste 13: Compilação Executável ===")
печать("Este código deve compilar para .exe")
печать("Versão: 2.4.9")

пусть contador = 0
для (пусть i = 0; i < 5; i = i + 1) {
    contador = contador + 1
    печать("Contador: " + contador)
}

печать("✅ Teste 13: Compilação Executável - PASSOU")
печать("Executável criado com sucesso!")
