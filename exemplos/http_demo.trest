# Demonstração HTTP - Cliente

печать("╔═══════════════════════════════════════╗")
печать("║   🌐 HTTP DEMO                        ║")
печать("╚═══════════════════════════════════════╝")
печать("")

# Importar módulo HTTP
импорт * как HTTP измодуля "std/index"

печать("Testando GET request...")

# Tentar fazer GET request
try {
    печать("Tentando buscar dados de API pública...")
    # Note: Require async/await implementation
} catch (error) {
    печать("Erro:", error)
}

печать("")
печать("📝 Criando servidor HTTP local...")

пусть server = HTTP.создатьСервер()

server.получить("/", (req, res) => {
    печать("📍 Requisição recebida em /")
    res.status(200).send("Olá do servidor Trest!")
})

server.слушать(8080, () => {
    печать("✅ Servidor rodando em http://localhost:8080")
})

печать("")
печать("Servidor configurado! (funcionalidade limitada sem async/await)")

