# Demonstração JSON

печать("╔═══════════════════════════════════════╗")
печать("║   📄 JSON DEMO                        ║")
печать("╚═══════════════════════════════════════╝")
печать("")

импорт * как JSON измодуля "std/json"

# Criar objeto
пусть usuario = {
    nome = "PoktWeb",
    idade = 25
}

печать("Objeto:", usuario)
печать("")

печать("JSON.stringify() disponível")
печать("JSON.parse() disponível")

печать("")
печать("✅ Demo JSON concluído!")

