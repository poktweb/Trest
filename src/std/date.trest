# Date Library - Data e Hora

# Obter data atual
экспорт функция now() {
    # Реализация nativa em src/std-native.ts
    бросить "now() requer implementação nativa."
}

# Obter timestamp atual (milissegundos desde epoch)
экспорт функция timestamp() {
    # Реализация nativa em src/std-native.ts
    бросить "timestamp() requer implementação nativa."
}

# Formatar data
экспорт функция format(date, formatStr) {
    # TODO: Implementar format
    # A implementação real é fornecida pelo módulo nativo
    вернуть ""
}

# Obter timezone
экспорт функция timezone() {
    # TODO: Implementar timezone()
    # A implementação real é fornecida pelo módulo nativo
    вернуть ""
}

