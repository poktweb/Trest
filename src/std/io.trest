# Стандартная библиотека ввода/вывода
# Примечание: Реализации используют нативные привязки Node.js

# Функция читать() реализована нативно
# Она читает одну строку из стандартного ввода (stdin)
экспорт функция читать() {
    # Реализация предоставляется нативным модулем
    # Эта функция читает ввод do usuário até encontrar Enter
    вернуть ""
}

# Функция печать() - эквивалент печать() global
экспорт функция печать(...args) {
    # Реализация предоставляется нативным модулем
    # Imprime valores no console
}
